<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PelayananDesa"
        tools:targetApi="31">
        <activity android:name=".activity.DomisiliActivity" />
        <activity android:name=".activity.BlmNikahActivity" />
        <activity android:name=".activity.SkuActivity" />
        <activity android:name=".activity.KeramaianActivity" />
        <activity android:name=".activity.LoginActivity"/>
        <activity android:name=".activity.RiwayatActivity"/>
<!--        <activity android:name=".activity.SignupActivity" />-->
        <activity android:name=".AkunActivity"/>
        <activity android:name=".SuratActivity"/>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".activity.SignupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>